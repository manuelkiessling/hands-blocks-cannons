{
  "name": "@gesture-app/lobby",
  "version": "1.0.0",
  "type": "module",
  "description": "Lobby for creating and managing gesture app sessions",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npm run build:frontend && npm run build:server",
    "build:server": "tsup src/index.ts --format esm --dts",
    "build:frontend": "vite build",
    "start": "node dist/index.js",
    "check": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@gesture-app/blocks-cannons": "*",
    "@gesture-app/framework-protocol": "*",
    "@gesture-app/hello-hands": "*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
